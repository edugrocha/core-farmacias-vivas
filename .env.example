SECRET_KEY=django-insecure-farmacias-vivas-2026
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

DB_NAME=FarmaciaViva
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=True

# ───────────────────────────────────────────
# Produção — só têm efeito com DJANGO_SETTINGS_ENV=production (ver
# config/settings/production.py). Em desenvolvimento, ignore estas linhas.
# ───────────────────────────────────────────
# DJANGO_SETTINGS_ENV=production
# SECURE_SSL_REDIRECT=True
# SECURE_HSTS_SECONDS=2592000
